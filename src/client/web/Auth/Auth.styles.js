export default theme => ({
  normalLayout: {
    background: theme.background.primary,
    height: '100vh',
  },
  normalLayoutContents: {
    background: theme.background.primary,
  },
  copyRightInfo: {
    textAlign: 'center',
  },
});
